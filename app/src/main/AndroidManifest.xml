<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.chrisfajardo.fix">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
        <activity
            android:name=".ui.PaintScreenActivity"
            android:label="@string/title_activity_paintscreem"
            android:theme="@style/NoActionBar"></activity>

        <activity android:name=".ui.PostActivity" />
        <activity android:name=".ui.ServicesActivity" />
        <activity
            android:name=".ui.SplashscreemActivity"
            android:theme="@style/splashThemme" />
        <activity android:name=".ui.HomeActivity" />
        <activity android:name=".ui.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.BuildingScreenActivity"
            android:theme="@style/NoActionBar"></activity>
        <activity
            android:name=".ui.LocksmithScreenActivity"
            android:theme="@style/NoActionBar"></activity>
        <activity
            android:name=".ui.CleaningScreenActivity"
            android:theme="@style/NoActionBar"></activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>